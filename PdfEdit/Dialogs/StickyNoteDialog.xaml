<Window x:Class="PdfEdit.Dialogs.StickyNoteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Sticky Note"
        Width="360" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF2D2D2D"
        Foreground="White">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF3A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="16,5"/>
            <Setter Property="Background" Value="#FF444444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF666666"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="16">
        <TextBlock Text="Note Text" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox x:Name="NoteTextBox"
                 Height="90"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,12"/>

        <TextBlock Text="Author" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox x:Name="AuthorBox" Margin="0,0,0,12"/>

        <TextBlock Text="Color" FontWeight="SemiBold" Margin="0,0,0,6"/>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <RadioButton x:Name="ColorYellow"  GroupName="NoteColor" IsChecked="True"  Margin="0,0,10,0">
                <RadioButton.Content>
                    <Border Width="24" Height="24" Background="#FFFF88" CornerRadius="3" BorderBrush="#BBBB00" BorderThickness="1"/>
                </RadioButton.Content>
            </RadioButton>
            <RadioButton x:Name="ColorGreen"   GroupName="NoteColor" Margin="0,0,10,0">
                <RadioButton.Content>
                    <Border Width="24" Height="24" Background="#88FF88" CornerRadius="3" BorderBrush="#00AA00" BorderThickness="1"/>
                </RadioButton.Content>
            </RadioButton>
            <RadioButton x:Name="ColorBlue"    GroupName="NoteColor" Margin="0,0,10,0">
                <RadioButton.Content>
                    <Border Width="24" Height="24" Background="#88AAFF" CornerRadius="3" BorderBrush="#2244BB" BorderThickness="1"/>
                </RadioButton.Content>
            </RadioButton>
            <RadioButton x:Name="ColorPink"    GroupName="NoteColor" Margin="0,0,10,0">
                <RadioButton.Content>
                    <Border Width="24" Height="24" Background="#FFB0C8" CornerRadius="3" BorderBrush="#CC4488" BorderThickness="1"/>
                </RadioButton.Content>
            </RadioButton>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Click="OkClick" Margin="0,0,8,0" Width="72"/>
            <Button Content="Cancel" IsCancel="True" Width="72"/>
        </StackPanel>
    </StackPanel>
</Window>
